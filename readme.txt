This is a sample file created to add a file within new github repositroy named WebDevFlask.

The repository is imported from github by Repli. Within Replit the programing will be done as Flask can run on cloud based environment.
